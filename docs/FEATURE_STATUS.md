# Canonical feature ledger

This is the source of truth for implementation progress. `IMPLEMENTED` means code and automated tests exist; `VERIFIED` additionally requires bidirectional upstream evidence. `WIP` names missing behavior, `PLANNED` has no complete implementation, and `BLOCKED` records an external dependency.

| Area | Status | Evidence and remaining work |
| --- | --- | --- |
| Built-in Guide screen | IMPLEMENTED | `G` opens a native keyboard/workflow guide, with narrow-terminal clipping, deterministic headless output and Escape/navigation isolation. Dispatcher and render-dump tests cover entry, content and return. Protocol tutorials and searchable long-form help remain. |
| Interfaces screen | IMPLEMENTED | `I` opens a screen-scoped read-only view populated only through the public runtime APIs. Every runtime interface shows its name, type, state, last error, packet/byte/drop totals, connection attempts/established/lost totals and peer count. Selection follows the interface's stable runtime ID across snapshot reordering, `j`/`k` scroll a selection-following compact viewport, and `r` takes an explicit fresh snapshot without restarting or mutating the network. Empty/offline states are explicit; pure model/formatter, narrow-width, headless dump and dispatcher tests provide local evidence only. Live counter and screen verification against upstream remains. |
| Privacy-safe event log screen | IMPLEMENTED | `L` opens a bounded 128-entry in-memory ring of distinct user-visible status transitions, including delivery, browser and runtime-startup results. The cursor follows new events only while it is already on the newest entry; j/k inspect retained entries and x clears the ring without affecting messages or protocol stores. The log deliberately excludes message plaintext, packet bytes and cryptographic material, and resets on restart. State, wraparound, selection, clear, headless output and screen-scoping tests exist. Persistent logs, continuous per-interface transition capture, severity/category filters and upstream UI evidence remain. |
| Configuration screen | IMPLEMENTED | `F` opens a bounded read-only view of the exact parsed Reticulum configuration: source path, validation diagnostic, core transport/share/error policy and the configured interface endpoints. Configuration input is capped at 64 KiB and paths are bounded before opening. Headless dump and screen-scoped dispatch tests cover valid, invalid and absent state. Editing, atomic apply and runtime restart remain WIP, and unsupported drivers remain labelled by the runtime rather than simulated. |
| Network-screen search | IMPLEMENTED | `/` opens a screen-scoped node filter matching case-insensitive names or destination fragments. Filtered results preserve registry ordering and destination-anchored selection; empty results cannot activate a hidden node. State/dispatcher and registry tests cover entry, editing, selection and no-match behavior. |
| Bounded node-registry search | IMPLEMENTED | A filtered sorted snapshot matches node names case-insensitively and destination-address fragments while retaining reachable/newest/hash ordering. Caller capacity bounds all copies. `test_node_registry` covers case folding, hex fragments, no matches and existing sort behavior. |
| Portable node-registry persistence | IMPLEMENTED | `RNSN3` uses explicit big-endian widths, bounded length-delimited records, CRC-32 corruption detection, 64-bit timestamp representations and atomic same-directory temporary write/fsync/rename. Record version 2 retains verified propagation enabled/cost metadata while version 1 remains readable; loaded nodes are still treated as stale until a fresh announce. Loads are transactional and retain the caller registry on corrupt/truncated input. Up to 256 opaque trailing bytes per record survive load/save for forward-compatible extensions. Migration accepts the local-ABI `RNSN1`/`RNSN2` formats only at their exact expected size, copies named fields without interpreting padding, validates every Boolean/length/kind/time, and does not convert the unusable RNSN1 ratchet-presence bit into a zero cryptographic key. `test_node_registry` covers every truncated prefix, checksum damage, atomic replacement/reopen, pre-write validation failure, full metadata and unknown-byte round trips, both migrations and malformed legacy Boolean data. This is local persistence evidence, not upstream interoperability. |
| Per-conversation drafts | IMPLEMENTED | Composer changes are atomically persisted in the versioned peer store and restored per contact after selection, screen changes and restart. Sending clears only that conversation's draft. An existing oversized peer-store draft is preserved until edited, then bounded to the 1 KiB composer. Tests cover independent contacts, restart, send clearing and preservation of untouched larger data. |
| LXMF URI and paper-message codecs | IMPLEMENTED | `lxmf_paper_pack/unpack` signs normal LXMF, encrypts `source || signature || payload` with the destination identity or advertised ratchet, prefixes the 16-byte destination, computes the transient SHA-256 ID, and verifies/decrypts with bounded private-ratchet history. `lxmf_uri_encode/decode` implements the pinned canonical `lxm://` URL-safe Base64 form without padding, the 2,210-byte paper/2,953-character QR limits, case-insensitive scheme parsing and bounded slash-separated scanner input. Router paper/URI import uses the shared destination, size, block, signature, unknown-sender retention, persistence and callback path; exact transient repeats and durable message-ID duplicates are suppressed, configured or caller-supplied ratchet histories can be enforced, and only inbound stamp cost is exempt as in pinned LXMF. `nomad-chat paper-export` produces a URI for an explicitly supplied verified destination key, and `paper-import` validates into the durable store with verified or explicitly unverified source handling; both have bounded human and JSON output tests, duplicate handling and safe oversize/malformed rejection. Tests cover verified/unverified/blocked/forged/wrong-destination/duplicate/persistent/ratcheted messages and prove an unstamped network packet is rejected while its paper form is accepted. The complete packed message preserves title, stamp and unknown MessagePack fields. Provenance-recorded fixtures produced by pinned LXMF `795fdaa2b0777c13033787d933d1afc94a2377cb` `LXMessage.as_uri(finalise=False)` cover 97-byte, binary 2,048-byte and maximum-size paper data; deterministic crypto/ratchet round trips, malformed/boundary tests and `fuzz_lxmf_uri` exercise the C implementation. The URI prefix is `lxm://`, not `lxmf://`, and there is no separate paper compression layer. QR rendering/scanning, CLI ratchet-history selection and live upstream encrypted-paper exchange remain. Fixture equality and local router/CLI tests are not full paper interoperability evidence. |
| LXMF router propagation upload | WIP | The durable caller-polled router can select or safely replace a verified propagation identity/hash/cost, prepare recipient encryption and ratchet, generate the exact 1,000-round PN stamp, and reuse the authenticated propagation session for a proof-tracked upload. It persists actionable prerequisite/stamp/Resource progress, maps node proof to SENT rather than DELIVERED, cancels durably, recovers interrupted state, and uses bounded exponential retry with a terminal `retry exhausted` reason. The TUI supplies only a fresh, reachable, enabled, cryptographically verified propagation announce with a valid advertised cost. C-to-C `test_lxmf_router_propagation` covers failure/recovery behavior. The opt-in pinned-Python UDP and continuous framed-TCP harness verifies one identified stamped upload, exact message/signature validation and node completion proof; see `LXMF_PROPAGATION_INTEROP.md`. Automatic scheduling, loss/reconnect, durable transient blobs and resume remain. |
| LXMF router propagation sync | WIP | An explicit caller-polled router operation reuses the propagation session for list/download/ack, validates transient hashes and the local destination, decrypts with identity/private-ratchet history and applies the shared signature, stamp, block, size and durable-store policy before acknowledgement. Durable message-ID duplicates are acknowledged after restart without invoking the delivery callback again; rejected messages stay on-node, explicit retain suppresses all acknowledgement, upload/sync and node replacement are serialized, and status distinguishes each phase, counts, remote/transport errors, partial local rejection and cancellation. C-to-C `test_lxmf_router_propagation_sync` covers failure and persistence behavior. The opt-in pinned-Python UDP and continuous framed-TCP harness verifies one 2 KiB list/download/ack exchange, exact signature/content/unknown-field retention, durable acceptance and deletion from the Python node only after acknowledgement; see `LXMF_PROPAGATION_INTEROP.md`. The TUI now exposes explicit start/cancel and truthful phase/count/error status. Automatic scheduling, durable session resume and loss/reconnect remain. |
| Live LXMF propagation UDP/framed-TCP client exchange | VERIFIED | The opt-in pinned-Python harness runs one stamped C upload and one Python-to-C `/get` list/download/ack sync against unmodified RNS `ea98db4` / LXMF `795fdaa`. On both loopback UDP and one continuous framed-TCP connection, Python accepts the identified upload and returns its completion proof; C verifies and durably stores the exact 2 KiB signed message with title and unknown binary field, acknowledges it, and the Python node removes it. Separate privacy-safe reports in `tests/fixtures/lxmf_propagation_udp_live.provenance.json` and `tests/fixtures/lxmf_propagation_tcp_live.provenance.json` record public IDs, states, counters and source/binary fingerprints. This does not verify reconnect, restart/resume, loss, relay hops, hosting/peering, automatic scheduling, TUI sync, multi-segment Resources or physical RNode. See `LXMF_PROPAGATION_INTEROP.md`. |
| Direct LXMF UDP packet/Resource exchange | VERIFIED | Opt-in `tools/test_lxmf_direct_live.py` runs the actual C router against clean pinned Python RNS `ea98db4` / LXMF `795fdaa` over loopback UDP, with temporary synthetic identities learned through verified announces. Both directions deliver a 17-byte packet and an incompressible 2 KiB, five-part Resource; exact content, source signatures, titles, unknown binary fields and final packet/Resource proofs are required. Public event evidence and source/binary fingerprints are in `tests/fixtures/lxmf_direct_udp_live.provenance.json`, captured from complete committed source `95d5977`; strict 64/64, live ASan/UBSan and focused sanitizer tests pass. The test exposed and now guards verified incoming metadata retention. This narrow exchange is verified, not general messaging parity: transport hops, reconnects, stamps, propagation, multi-segment resources and TUI workflows remain unverified. Framed TCP has its own evidence row. See `LXMF_DIRECT_INTEROP.md`. |
| Direct LXMF framed-TCP packet/Resource exchange | VERIFIED | The same opt-in harness runs the native TCP client against pinned Python RNS 1.5.2 `TCPServerInterface` and LXMF 1.1.0. On 2026-09-03 both directions validated verified identities, signatures, exact synthetic 17-byte and incompressible 2 KiB content, titles, unknown binary fields, link packet proofs and five-part Resource completion proofs. `tests/fixtures/lxmf_direct_tcp_live.provenance.json` records public message IDs/states, exact upstream revisions, committed native source `1723bdc`, source/binary SHA-256 values and no plaintext, keys, histories or captures. The acceptance run exposed a responder echoing Python's 16 KiB stream MTU into 500-byte local framing; `5e51f2f` now signs a safe negotiated downgrade and `test_link` guards it. This verifies one continuous loopback TCP connection only—not reconnect, relay hops, loss, stamps/tickets, propagation, multi-segment Resources or TUI workflows. See `LXMF_DIRECT_INTEROP.md`. |
| Complete inbound LXMF representation retention | IMPLEMENTED | Accepted verified messages now retain exact packed bytes just like unverified messages, rather than discarding title, arbitrary fields, attachments/media metadata and stamps after signature verification. `test_lxmf_router_stamps` checks verified opportunistic title, unknown extension and ticket stamp against decrypted bytes through compaction/reopen; `test_lxmf_router_direct` checks full verified 2 KiB Resource bytes, title, unknown fields and proof-of-work stamp. Signature and inbound-policy regressions cover known/unknown senders and unchanged size rejection. Strict Clang 64/64 and focused ASan/UBSan 4/4 pass. The existing 8 MiB packed bound, 16 MiB journal quota, 4 KiB content-preview admission limit and Resource segment limit remain; already discarded metadata cannot be recovered. Live upstream metadata verification remains pending. |
| LXMF standard fields | IMPLEMENTED | Bounded native parse/merge APIs cover pinned renderer, reply target/quote, reaction, thread, file attachment, image and audio constants and shapes. Known values enforce full message-ID hashes, UTF-8 and explicit count/name/data limits. Replacing or removing a standard field copies every untouched MessagePack key/value pair byte-for-byte, including unknown extensions. A traversal-safe filename helper never writes automatically. Pinned LXMF/RNS provenance and exact `umsgpack` fixture equality are recorded in `tests/fixtures/lxmf_standard_fields.provenance.json`; unit, malformed/truncation and fuzz-entry tests cover the parser and constructor. This is deterministic fixture evidence, not live upstream interoperability. Conversation metadata rendering and explicit attachment-save UI remain WIP. See `LXMF_STANDARD_FIELDS.md`. |
| Saved block preferences in messaging | IMPLEMENTED | The TUI binds its saved block flag to the LXMF source-policy callback, including persisted peers outside the 64-contact display cache. Trust never implicitly blocks or bypasses stamps. Deferred policy rejections remove the cached unverified row and update unread counts without labelling signatures invalid; prior accepted history remains. `test_tui_state` covers toggles, trust separation, reopen/fallback and retained neighbouring messages. Live blocked-peer upstream evidence remains. |
| Conversation preview ownership | IMPLEMENTED | The application cache bounds/copies text and never retains callback-scoped packed-message spans. Removing a rejected message rebinds relocated inline preview pointers, preserving subsequent text. `test_tui_state` exercises repeated rejection before distinct surviving messages. Full representations remain owned by the durable LXMF journal. |
| Screen-scoped TUI input | IMPLEMENTED | `tui_dispatch_key` confines selection, scrolling, conversation actions and editors to their owning visible screens; empty Network and filtered-out conversations cannot activate hidden composers. Stale editors/modals consume the triggering key without applying data or clearing draft text. Node popup selection recovers after expiry. Help is a visible Conversations modal or screen-specific status guidance, not an invisible overlay. Escape closes modal/editor first, cancels an active Browser request once, then returns to Conversations; only root Escape or explicit quit exits. `test_tui_dispatch` exercises all eight screen enums, empty/hidden selections, stale fields, modal isolation, draft retention, node-message action, idle/cancelled Browser Escape and resize/no-input events without initializing curses. This is input-safety coverage, not complete screen or NomadNet UI parity. |
| LXMF inbound block and size policy | IMPLEMENTED | The core router checks an application-owned source-block callback before history, ticket learning, delivery callbacks or link-packet proofs. It also applies one packed-message limit to packets, Resource advertisements and deferred verification. Policy rejection uses `LXMF_ERR_BLOCKED`, not a false signature failure; trust does not bypass stamps. The TUI binds this API to its saved block flag, including peers outside its display cache. Unit and C-to-C direct tests cover verified/unknown sources, changed policy, exact size boundaries, Resource rejection and withheld proof. Live blocked-peer evidence remains; Resource completion proofs acknowledge transport completion before application filtering. |
| Propagation client session | WIP | Opaque caller-polled client verifies selected node identity/destination, discovers paths, establishes/identifies links, uploads proof-tracked Resources, lists bounded transient IDs, downloads in packet-safe batches, and acknowledges only application-confirmed durable messages. Matching hashes and duplicates are checked before callbacks; cancellation, per-operation deadlines, remote errors, retain-on-node and progress are exposed. The LXMF router integrates upload and explicit inbound sync, recipient encryption/decryption, policy and durable message-ID replay handling; the TUI exposes manual nonblocking start/cancel and status. `test_lxmf_propagation_session` covers local UDP multi-batch/resource-response sync, storage rejection, malformed IDs/content, access denial, upload, cancellation and timeout, while pinned-Python UDP/TCP evidence is recorded separately. Automatic scheduling, durable session resume and multi-segment transfer remain. See `PROPAGATION_CLIENT.md`. |
| Static hosted Nomad pages | IMPLEMENTED | Opaque opt-in POSIX service publishes explicitly selected `/page/` paths on an authenticated `nomadnetwork.node` destination, with binary packet/Resource responses, fresh bounded reads, explicit announces, global identification/allowlist policy, per-page regular `.allowed` identity lists and owned-link cleanup. Policies are descriptor-walked without symlinks, bounded to 64 exact identity hashes, reread per request and fail closed when malformed/oversized/executable. `test_hosted_node` rejects traversal, hidden paths, symlinks, executable sources and oversized files; `test_hosted_node_link` exercises C-to-C UDP identification, global allowlist, dynamic sidecar allow/deny/malformed states, 2 KiB Resource and updated small packet pages. Local file-root reads exist, but remote file streaming metadata, executable pages/policy generators, form environments, scans, UTF-8 paths, multi-segment transfers and application/daemon controls remain WIP. See `docs/HOSTED_NODE.md`. No upstream network verification claimed. |
| Hosted page form decoder | IMPLEMENTED | `rns_hosted_form_decode` validates exactly one bounded MessagePack map without allocation, retains callback-scoped scalar `field_`/`var_` values, skips structurally valid unknown fields, rejects composite retained values and trailing/truncated input, and gives duplicate keys final-value semantics. `test_hosted_form` covers canonical string, binary, Boolean, signed, unsigned, floating and nil scalars, nested ignored data, non-string keys, map16/map32, alias ownership, duplicates and bounds. It does not construct process environment strings or execute pages. No upstream network verification claimed. |
| Executable hosted Micron pages | BLOCKED | The pinned implementation executes a page path directly. A secure descriptor-pinned equivalent is not portable across the initial POSIX targets: macOS does not expose `fexecve`, and executing `/dev/fd` is unavailable. Falling back to a validated pathname would reintroduce a file-swap race. No executable-page code is retained; this needs an isolated helper/sandbox design with an explicit interpreter policy, deadlines and output caps before implementation. |
| Live short opportunistic UDP wire exchange | VERIFIED | `tools/test_lxmf_live_python.py` passes C → pinned Python `LXMRouter` and pinned Python `LXMessage.send()`/RNS UDP → C `receive-udp`, checking signatures, source/destination and exact synthetic content. `docs/LXMF_LIVE_OPPORTUNISTIC.json` records the 2026-09-03 run, exact RNS 1.5.2/LXMF 1.1.0 commits, declared native source commit, executable SHA-256, message IDs and payload hashes without plaintext/keys. This narrowly verifies identity-key short packets with preprovisioned public identities over loopback UDP, not announces, ratchets, stamps, receipts, router queues, links/resources, propagation, TCP or TUI delivery. See `docs/LXMF_LIVE_TESTING.md`; harness is opt-in and eight network-free safety tests cover pin enforcement, process cleanup and sanitizer diagnostics. |
| Propagation wire codecs | IMPLEMENTED | Allocation-free bounded codecs cover LXMF 1.1.0 seven-field propagation announces (unknown metadata/outer fields retained), uploads, `/get` list/download/acknowledgement requests, ID/message responses, integer errors and upload stamp rejection. Limits preserve decimal kilobytes, null differs from empty lists, transient IDs require 32 bytes, and opaque encrypted message bodies are not mistaken for validated LXMF. `test_lxmf_python_propagation` compares provenance-recorded synthetic fixtures generated by pinned upstream producers, exact byte re-encoding, truncation/mutation and bounds tests. This is offline codec evidence only, not bidirectional network verification. Propagation sessions, encryption/stamp handling, peering, upload/download scheduling, persistence and node hosting remain WIP. |
| C17 build, HAL, buffers, status API | IMPLEMENTED | Warning-as-error builds and unit tests. Embedded adapters remain planned. |
| Crypto, identities, destinations, packets | IMPLEMENTED | Known-answer and unit tests. Identity encryption supports announced X25519 ratchets, ordered private-ratchet decryption, ratchet IDs, identity-key fallback and strict ratchet enforcement. A bounded signed MessagePack ratchet store persists up to the upstream-default 512 private keys, rotates at the upstream-default 30-minute interval, rejects tampering and copies decryption keys newest-first. The runtime can sign the current ratchet into announces. The full upstream vector matrix remains WIP. |
| Signed announces and paths | WIP | Build/parse/verification, verified runtime callbacks and path snapshots tested. Verified events expose the borrowed public ratchet bytes, and the bounded registry copies ratchets plus decoded LXMF delivery metadata and propagation-node enabled/cost metadata while retaining original announce app data. Legacy and portable record migrations leave new propagation fields unset. Persistent discovery and upstream bidirectional capture remain. |
| UDP and TCP interfaces | IMPLEMENTED | Loopback tests, setup guide and a reachability-checked public uplink configuration. The caller-polled TCP client now completes non-blocking connects only after socket readiness, reports STARTING/DOWN/UP transitions, and retries refused or dropped connections with bounded exponential backoff. The TCP server recovers a sequential listening slot after peer disconnect; lifecycle counters expose outbound attempts, established peers and losses. `test_runtime_tcp_reconnect` deterministically covers refused startup followed by recovery, a dropped live client followed by reconnect, and acceptance of a second sequential server peer. Pending endpoint bytes are discarded on disconnect instead of replaying a possibly partial HDLC frame; durable upper-layer queues remain responsible for retry. Concurrent TCP server peers, configurable upstream fixed reconnect timing, TCP keepalive/socket tuning and pinned-upstream lifecycle evidence remain. UDP receive tests use monotonic deadlines instead of scheduler-sensitive iteration budgets (`test_udp`, `test_udp_lxmf`). |
| Shared-instance local IPC | IMPLEMENTED | Parsed upstream-style `share_instance`, `shared_instance_type`, `instance_name`, `shared_instance_port` and `instance_control_port` settings now drive a caller-polled loopback TCP interface. AUTO election creates one bounded multi-client owner or attaches as a client; attached runtimes disable duplicate configured system interfaces, use exact HDLC packet framing, expose connection/packet/byte/drop counters and error states, and reconnect after owner restart with bounded backoff. `test_local_instance` covers election, bidirectional frames, multiple clients and restart recovery; `test_runtime_local` covers runtime delivery and statistics. `instance_data_port` remains a legacy alias. This is local C-to-C evidence only: AF_UNIX abstract sockets, control/RPC, shared path/cache synchronisation and pinned-Python interoperability remain. |
| KISS serial interface | IMPLEMENTED | A public caller-polled POSIX endpoint opens and configures bounded serial paths without blocking, emits pinned TXDELAY/TXTAIL/P/SLOTTIME/READY commands, streams escaped port-tagged KISS frames, handles partial I/O, READY flow control with the pinned timeout, bounded transmit/receive queues, lifecycle counters and five-second reconnect. Runtime configuration accepts the pinned port/speed/data bits/parity/stop bits/radio timing/flow-control fields and defaults. `test_framing`, `test_config`, `test_kiss_serial` and `test_runtime_kiss` cover command frames, malformed escapes, fragmentation, flow gating, pseudo-terminal disconnect/reconnect and runtime ingress/egress. `tests/fixtures/kiss_serial.provenance.json` records the pinned behavioral source studied. This is deterministic simulated C evidence only: live pinned-Python TNC exchange, serial-device diversity, beacon identification and physical-radio validation remain unverified. |
| RNode serial interface | IMPLEMENTED | A public caller-polled POSIX endpoint implements the full-byte escaped RNode host command stream separately from nibble-masked KISS, fixed 115200 8N1 serial opening, pinned delayed detection, minimum firmware 1.52 enforcement, radio configuration and reported-state validation, READY flow gating, bounded queues/partial I/O, hardware errors, metadata, leave/off signalling and five-second reconnect. Startup validation intentionally matches pinned RNS 1.5.2: bandwidth, TX power, spreading factor and radio state gate readiness; frequency is checked only when reported; coding-rate and configured airtime-lock echoes do not gate readiness. Runtime configuration validates serial-only ports, frequency, bandwidth, TX power, spreading/coding rates and optional short/long airtime locks; TCP/BLE RNode URI transports are rejected explicitly. `test_rnode_serial`, `test_config` and `test_runtime` use a deterministic pseudo-terminal peer to cover those compatibility omissions plus rejected bandwidth mismatch, detection/configuration echoes, old firmware rejection, escaped fragmented packets, malformed input, READY queuing, saturated writes, disconnect/reconnect and runtime egress. `tests/fixtures/rnode_serial.provenance.json` records the pinned behavioral source reviewed. This is simulated local evidence only: physical Rei hardware, live Python exchange, device statistics/display/framebuffer, identification beacons, TCP/BLE transports and multi-interface RNodes remain unverified. |
| AutoInterface | IMPLEMENTED | A bounded caller-polled protocol engine implements the pinned SHA-256 group/address discovery token, derived IPv6 multicast scope/type, 1.6-second beacons, 5.2-second reverse peering, 22-second expiry, raw datagram carrier and 48-entry/0.75-second multi-interface duplicate suppression. The POSIX runtime adapter enumerates IPv6 link-local devices, applies bounded allow/ignore lists, joins multicast, exposes nonblocking startup/runtime state and fans packets to current peers. Config parse/emit covers group, scope, multicast type, discovery/data ports, device filters and bitrate. `test_auto` uses injected clock/I/O and pinned vectors from `auto_interface.provenance.json`; `test_config` covers round trips. This is deterministic C evidence, not upstream verification. Dynamic device/address changes, multicast-echo carrier diagnostics, IFAC, interface-mode/announce controls, MTUs above the current 500-byte core packet ceiling and live Python exchange remain. See `AUTO_INTERFACE.md`. |
| Transport forwarding | WIP | Basic ingress, forwarding and dedupe exist. A bounded reverse table records successfully forwarded ordinary header-2 transport packets by truncated packet hash and returns context-none header-1 explicit or implicit proofs only from the recorded downstream interface. Entries use the injected monotonic clock, expire at the pinned 480-second default, are consumed on proof attempts, and evict oldest-first at capacity; runtime forwarding now selects the path's exact interface instead of flooding routed packets. `test_reverse_path` covers one deterministic transport hop, header stripping, explicit/implicit proofs, wrong and expired paths, duplicates, malformed/ineligible proof frames and bounded eviction. `rns_reverse_path.provenance.json` records the pinned RNS 1.5.2 source audit. Local ordinary-packet receipts validate proofs, report RTT/timeouts/cancellation, and can emit a proof on the receiving interface. A versioned, checksummed, allocation-bounded path snapshot codec transactionally restores routes while deducting offline wall time; reverse and dedupe state are not restored. Application storage wiring, link/tunnel tables and bidirectional pinned-upstream one-hop evidence remain; this is not full transport parity. |
| Links and channels | WIP | Handshake crypto, bounded channel state, and caller-polled outbound and inbound runtime link lifecycles exist. Identity-associated inbound destinations validate link requests, return signed LRPROOFs, authenticate RTT confirmation and encrypted packets, and use protocol-correct raw keepalives. Responders cap a peer-proposed stream MTU to the local 500-byte packet/framing bound and confirm that safe value in the signed proof, preventing oversized Resource parts; `test_link` covers the negotiated downgrade from 16 KiB. Encrypted application packets support explicit ephemeral-key proofs, initiators can privately identify, and active links dispatch single-segment Resource control/data frames. Provenance-recorded pinned Python fixtures validate deterministic link-request/LRPROOF packet bytes, link IDs, proof and identification signing preimages, signatures, signalling, and RTT MessagePack plaintext; deterministic C-to-C UDP tests cover the runtime foundation. Random-IV ciphertext, backchannel routing and multi-hop reverse paths remain. |
| Requests and handlers | IMPLEMENTED | Authenticated inbound destinations register bounded opaque request handlers by the upstream SHA-256 path prefix with allow-none, allow-all, identified-peer and copied identity-allowlist policies. Valid request MessagePack reaches callback-scoped immutable views; arbitrary single-object responses are correlated to the received packet hash and sent as encrypted packets or single-segment Resources. C-to-C UDP tests cover identified access, packet response and 2 KiB Resource response; unit tests cover arbitrary response objects and malformed rejection. Deferred handlers, concurrent response Resources, multi-segment responses and pinned-upstream live exchanges remain. |
| Resources | WIP | The bounded sender and receiver now separate the 74 hashes carried by an advertisement from the full segment map, exchange pinned `RESOURCE_HMU` MessagePack map pages, request/retransmit bounded windows, tolerate duplicate and reordered parts/updates, and reject conflicting, truncated or out-of-range map data. Payloads up to 8 MiB advance through independently compressed, encrypted, hashed and proved roughly-1-MiB segments with sequential advertisements and original-hash correlation; runtime links retain partial assembly between segments and clean it on cancellation, timeout or link failure. Advertisement admission requires every unique pinned field (including nil-or-16-byte binary `q` and binary `m`), rejects string-encoded binary fields, validates feasible part counts before allocation, and only accepts segment one as a new runtime transfer. Deterministic `test_resource_sender` covers a greater-than-2,200-part first segment, multiple HMUs, reordering, duplicates, malformed/conflicting updates, impossible allocation claims, duplicate/missing/type-confused fields, two segment proofs and exact reassembly; `test_runtime_resource` covers the same two-segment path plus out-of-sequence initial segments over local UDP in addition to rejection, cancellation, malformed control frames, timeout and teardown. `tests/fixtures/resource_multisegment.provenance.json` records the pinned RNS 1.5.2 behavior reviewed. Adaptive windows/retry timing, persistent resume, metadata-bearing segmented files, very-low-MTU transfers and pinned-Python exchange remain; this is local C evidence only. |
| LXMF codec, signatures, stamps, tickets | WIP | Core codec and unit tests exist. Canonical pack/unpack uses bounded heap workspaces up to 8 MiB instead of the former hidden 4 KiB stack ceiling. Delivery announce parsing retains bounded unknown supported-function identifiers and extension MessagePack objects for lossless re-encoding; registry tests cover verified ratchet and advertised stamp/compression metadata retention. Provenance-recorded fixtures generated by pinned Python LXMF 1.1.0 validate delivery announce data plus exact signed message representations, IDs, signing preimages, UTF-8 title/content, renderer/reply/reaction and unknown fields, the 255-byte binary boundary, and destination-prefix-elided opportunistic payloads. A bounded, versioned, atomic ticket store issues, throttles, retains, expires and validates ticket-backed stamps using the pinned lifecycle constants. The router now parses bounded `FIELD_TICKET` values from verified messages, remembers valid unexpired tickets, and reuses them without dropping unknown fields. Random Reticulum ciphertext and live ticket exchanges remain unverified. |
| LXMF delivery and propagation | WIP | Direct delivery registers an authenticated `lxmf.delivery` destination, reuses links, and selects packets or bounded single-segment Resources. Opportunistic delivery matches pinned Python. Packet, direct Resource and propagated-sync receive paths share destination, message-ID, signature, duplicate, block, size and stamp validation; accepted representations retain exact bytes. Proof and transfer state is durable. `LXMF_DIRECT_INTEROP.md` verifies packet and five-part Resource delivery with proofs in both directions against pinned Python over UDP and framed TCP; `LXMF_LIVE_TESTING.md` separately verifies short opportunistic packets. The propagation session is connected to explicit router upload and inbound sync, but not automatic scheduling or TUI sync. Relay hops, restart/reconnect, live stamps/tickets/ratchets, multi-segment Resources and live propagation remain unverified. |
| Caller-polled stamp worker | IMPLEMENTED | Opaque job lifecycle incrementally prepares all 3000 HKDF rounds and searches candidates in caller-selected budgets capped at 64 work units per poll. Incremental SHA-256 prefix state replaces a materialized 768 KiB workblock. The router schedules one bounded job, prefers valid tickets, persists completed same-ID stamped representations, reports progress, and preserves explicit cancellation across restart. The Nomad client resolves costs only from verified LXMF delivery app-data and displays preparation or search progress. Deterministic tests cover schedules, validation, competition, changed costs, tickets, cancellation, recovery and verified-cost selection. Resumable unfinished work and pinned Python/live evidence remain. |
| Router outbound stamp queue | IMPLEMENTED | A verified peer-cost resolver selects bounded proof-of-work before network use; valid unexpired tickets bypass work. One worker keeps messages QUEUED/STAMP, reports rounds/attempts, handles changed costs, and does not consume network retries. Completed stamps durably replace the same ID while preserving title and unknown fields. Cancellation survives reopen; explicit send retries it. The TUI resolves cost only from verified announce data and renders progress. Deterministic router and app tests cover scheduling, persistence and UI routing. Resumable unfinished work, legacy content-only stamping, quota compaction and live Python proof-of-work remain. |
| Peer and message persistence | IMPLEMENTED | Crash-recovering message journal and durable peer settings tests. Version-4 put records retain exact opaque packed representations up to 8 MiB with 32-bit lengths; legacy v1/v2/v3 records migrate on compaction. Streaming writes and CRC scans use bounded small buffers, and a size-query API permits exact heap allocation for reads, router sending and deferred verification. Tests cover 65,537-byte and 8-MiB reopen/compaction round trips, legacy delivery metadata, malformed lengths, small read buffers and oversize rejection. Message status, signature, title/unknown fields, delivery method/reason/attempt/progress/proof state survive unchanged. The 16-MiB journal quota and 4-KiB content preview limit remain; storage capacity does not imply equivalent codec or network capacity. |
| Nomad conversations | WIP | Verified-peer direct/opportunistic messaging, persistent trust/pin/note/block state, search, address entry, node-inbox handoff, a UTF-8 composer, history and offline queue exist. Direct remains the default; a screen-scoped `d` action explicitly chooses propagation-node upload for subsequently queued messages, persists that desired method per message and never silently downgrades it. Router receipt/signature/progress events update the live view; propagation-node acceptance is labelled SENT without claiming recipient delivery. Manual propagation download sync preserves conversations and accepted history on partial rejection, cancellation and runtime failure. Saved blocks are enforced in the LXMF layer, and valid identity announces revalidate retained unknown senders. Headless state/dispatch tests cover selection and truthful status. Complete end-user workflow evidence, attachments, replies, reactions, notifications and URI/QR UI remain. |
| Settings and self-announcement | WIP | The TUI has bounded, versioned, atomically replaced app settings for display name, enforced and advertised inbound stamp cost, announce-at-start, a minimum 30-minute periodic interval, propagation-node selection and manual announce. A saved propagation address is inert until the registry receives a fresh verified `lxmf.propagation` announce declaring the node enabled with cost 1-254; persisted nodes restart stale. Settings exposes manual Sync Now/cancel with nonblocking phase, message progress, accepted/duplicate/rejected/acknowledged totals and transport errors; no sync operation is persisted or auto-started after restart. Announces use the core LXMF app-data encoder and runtime API, rotate/persist the delivery ratchet and sign its public key into the announce. Automatic propagation sync and upstream screen-level evidence remain. |
| Network screen and active nodes | WIP | The configured runtime consumes verified announces, associates Nomad nodes with LXMF inboxes, decodes bounded propagation enabled/cost data, renders sorted persistent records, refreshes paths, and opens nodes through the caller-polled browser. Selection is held by destination across sorting/scrolling; the detail dialog can select only a fresh reachable enabled propagation announce with a valid cost, then starts/cancels manual sync only for that saved node. Headless state/dispatch tests cover empty/expired selections, modal isolation, selection/sync policy and runtime failure. Search, richer diagnostics, propagation peer management and upstream screen-level verification remain. |
| Native Micron browser | WIP | A caller-polled remote fetch state machine performs route discovery, authenticated link establishment, and packet- or resource-backed page retrieval. The bounded parser and curses renderer support sections, headings, latched emphasis and colours, alignment, dividers, links, and basic form-field display. Link navigation covers full destination URLs, same-node `:/page/...` links, relative paths and anchors in URL history. Tables and partials are detected but flattened; form editing/submission, in-page anchor positioning, caching and upstream page tests remain. |
| Channels/RRC and remaining screens | WIP | The caller-polled RRC session has an app-owned basic screen: uppercase `R` opens screen-scoped hub-address, public-identity, nick, room, reconnect and message controls; Connect uses the shared runtime without constructing protocol bytes in the app; live discovery/link/HELLO/WELCOME/reconnect state and negotiated limits are displayed; and join, part, send plus bounded sanitized incoming text are wired. Hub details, nick, last room, reconnect preference and unsent draft now survive restart in the atomically replaced application settings sidecar. Settings v1 is migrated to bounded CRC-protected v2 TLVs, unknown v2 records survive load/save, and malformed or invalid edits do not replace live values. Length-delimited text records reject embedded NUL bytes instead of silently truncating hidden suffixes. `test_tui_settings` covers round-trip, v1 migration, unknown retention, embedded-NUL rejection and transactional malformed nonreplacement; `test_tui_settings_ui` covers headless restart/draft recovery; `test_tui_dispatch` covers navigation, editing isolation, selected-row visibility calculations for small terminals and deterministic output. Existing session/fixture/runtime tests retain encrypted C-to-C evidence. This remains WIP, not live upstream verification: persistent room/member/message history, Resource envelopes, rate enforcement, commands/operator events, live Python hubs, hosted hub operation and the complete RRC UI are missing. Interfaces, Config and Guide are tracked separately; Logs remains unavailable. Unknown outer envelope keys are skipped, not retained. |
| RRC pinned-schema UDP exchange | VERIFIED | The opt-in `tools/test_rrc_live.py` harness runs the native caller-polled RRC client over UDP against pinned Python RNS 1.5.2 and uses NomadNet 1.2.0's unmodified `_make_envelope()` and vendored CBOR module in a fixture hub. It requires authenticated link identification, exact HELLO/WELCOME, JOIN/JOINED, bidirectional text fields with message-ID correlation, PING/PONG, PART/PARTED and disconnect. This caught and now guards wire timestamps using monotonic uptime instead of NomadNet's Unix epoch milliseconds. The wall-clock provider is injectable, its exact epoch value and typed failures have deterministic coverage, and monotonic caller time remains isolated to deadlines. Evidence and fingerprints are recorded in `tests/fixtures/nomadnet_rrc_udp_live.provenance.json`; see `RRC_INTEROP.md`. NomadNet does not ship an RRC hub, so this is not stock `rrcd` verification. Resource envelopes, reconnect, relay hops, TCP and complete room/history workflows remain unverified. |
| CLI utilities | WIP | `rnid`, `rnsd`, and `nomad-chat` history/node diagnostics exist, including JSON node output. Core utility parity remains. |
| Embedded portability | PLANNED | HAL boundary exists; cross-build examples and non-OpenSSL provider remain. |
| Upstream interoperability | WIP | Privacy-safe reports verify short opportunistic packets, direct packet/five-part Resource messages, one stamped propagation upload plus list/download/ack sync, and the narrow RRC pinned-schema exchange bidirectionally against pinned Python components over loopback UDP. Direct messaging and the same propagation exchange are also verified over continuous framed TCP. These are narrow gates only. One-hop transport, loss/reconnect, tickets/ratchets, pages/forms/files, external RRC hub deployments, remaining interfaces and physical RNode evidence are still required before a parity claim. |

The compatibility revisions and update policy are recorded in `COMPATIBILITY.md`.
